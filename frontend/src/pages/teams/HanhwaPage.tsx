import {TEAMS} from "@/shared";
import {TeamsLayout} from "@/pages/teams/TeamsLayout.tsx";

export const HanhwaPage = () => {

    return (
            <TeamsLayout team={TEAMS.HANHWA.KEY}/>
    );
};