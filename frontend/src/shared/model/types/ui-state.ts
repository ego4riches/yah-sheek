export interface IsOpenI {
    isOpen: boolean;
}

export interface IsExpandedI {
    isExpanded: boolean;
}

export interface IsVisibleI {
    isVisible: boolean;
}

export interface IsSelectedI {
    isSelected: boolean;
}

export interface IsNumberIdI {
    isId: number;
}