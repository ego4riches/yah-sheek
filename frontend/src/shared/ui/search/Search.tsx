import {SearchBox, SearchInputBox} from "@/shared";

export const Search = () => {

    return (
            <SearchBox>
                <SearchInputBox type="text" placeholder="Search..."/>
            </SearchBox>
    );
};